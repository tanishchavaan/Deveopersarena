package com.ecommerce.security.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // Additional generic JWT configurations isolated from SecurityConfig
}
